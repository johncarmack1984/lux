# AWS IoT Core — remote control (the lux-discord-bot Lambda publishes here).
# Endpoint + device cert/key come from `terraform -chdir=infra output`;
# AmazonRootCA1.pem is Amazon's public root CA. Leave unset to run the app
# without remote control.
AWS_IOT_ENDPOINT=
AWS_IOT_CERT_PATH=./certs/device.pem.crt
AWS_IOT_KEY_PATH=./certs/device.private.key
AWS_IOT_ROOT_CA_PATH=./certs/AmazonRootCA1.pem
LUX_DEVICE_ID=lux-1

# Accounts + cloud sync (Cognito + DynamoDB). The production endpoints are baked
# into the binary (see `DEFAULT_*` in src/account.rs), so these are optional
# overrides: set them (values from `terraform -chdir=infra output`) to point the
# app at a different pool / sync API, e.g. your own dev stack. Leave unset to use
# the shipped production backend.
COGNITO_USER_POOL_ID=
COGNITO_APP_CLIENT_ID=
COGNITO_REGION=
LUX_SYNC_URL=
